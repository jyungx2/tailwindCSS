{
  "scripts": {
    "start": "run-p watch serve",
    "dev": "tailwindcss -i ./src/styles/input.css -o ./src/styles/main.css",
    "watch": "npm run dev -- --watch",
    "serve": "live-server --open=/src/ --port=8090 --host=localhost"
  },
  "dependencies": {
    "pnpm": "^9.9.0"
  },
  "devDependencies": {
    "live-server": "^1.2.0",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.4.10"
  }
}